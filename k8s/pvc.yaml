---
# Holds the SQLite database at /data/casbin-gateway.db.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: casbin-gateway-data-pvc
  namespace: casbin-gateway
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
