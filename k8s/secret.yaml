---
apiVersion: v1
kind: Secret
metadata:
  name: casbin-gateway-secrets
  namespace: casbin-gateway
type: Opaque
stringData:
  # Casdoor credentials - REQUIRED: Replace these with your actual values before deployment
  # Get these from your Casdoor application settings
  casdoor-client-id: "CHANGEME_INSECURE_DEFAULT_REPLACE_THIS"
  casdoor-client-secret: "CHANGEME_INSECURE_DEFAULT_REPLACE_THIS"
